# tools/AGENTS.md

`tools/` is for optional local external binaries or helper tools.

## Rules

- Nothing in `tools/` is required for the default edudoc workflow unless explicitly approved.
- Do not add large binaries without user approval.
- Do not make tools mandatory for default usage.
- Document optional tool usage in `docs/`.