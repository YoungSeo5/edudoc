# --- 내 코드가 직접 쓰는 것 ---
watchdog>=4.0        # 폴더 watcher (connectors/folder_watcher.py)

# --- 입력 변환 (X -> Markdown, 소스 복사 금지·의존성으로만 참조) ---
python-hwpx          # .hwpx -> Markdown (HwpxDocument.export_markdown)
lxml                 # XML 파싱 (python-hwpx 의존)

# HWP(OLE 바이너리) -> HTML -> Markdown
pyhwp                # .hwp 파싱. import명은 hwp5. Python 3.13 동작 확인(pyhwp vs pyhwp5 상충 해소)
markdownify          # pyhwp HTML -> Markdown
# olefile 은 pyhwp 의존성으로 자동 설치됨

# --- 출력 생성 (Markdown -> 산출물, pip-native 기본 엔진) ---
markdown-it-py       # Markdown 공통 블록 파싱 (모든 exporter 공유)
python-docx          # -> DOCX
reportlab            # -> PDF (한글 시스템 폰트 등록 필요)
python-pptx          # -> PPTX
# HWPX 출력은 위 python-hwpx 로 생성
# Pandoc/typst 는 pip 대상 아님 - 선택 fallback(고충실도/비교). 기본 흐름은 위 pip 패키지만으로 동작.

# hwp2md(Rust)는 pip 대상 아님. cargo로 별도 빌드 후 바이너리 경로를 config에 지정.
