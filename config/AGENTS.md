# config/AGENTS.md

`config/` contains local and default configuration files.

## Rules

- Do not store secrets.
- Do not store private machine paths.
- Do not store generated outputs.
- Keep defaults portable.
- Environment-specific values should be documented, not hardcoded.